import React from 'react';
import Forgot from '../components/ForgotPassword';

const ForgotPassword = props => {
  return (
    <>
      <Forgot navigation={props.navigation} />
    </>
  );
};

export default ForgotPassword;
